<template name="mallTemplate">
    <view class="mall-container" catchtap="onMallTap" data-movieId="{{mallId}}">
        <image class="mall-img" src="{{coverageUrl}}"></image>
        <text class="mall-title">{{title}}</text>
        <text class="mall-discount">{{average}}折起</text>
        <!-- <template is="starsTemplate" data="{{stars:stars, score: average}}" /> -->
    </view>
</template>